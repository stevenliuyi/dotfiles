# basic shortcuts
alias ll="ls -l"
alias la="ls -a"
alias ..="cd .."
alias ...="cd ../.."
alias df="df -H"

# vim
alias vi="vim"

# g++
alias g++="g++ -std=c++14 -Wall -g -O2"

# tmux
alias tmux="tmux -2"

# trash
if which trash > /dev/null; then
    alias rm=trash
fi
